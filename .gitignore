# .NET
bin/
obj/
*.db
*.db-shm
*.db-wal
appsettings.Development.json
.vs/
*.user

# Node / Angular
node_modules/
dist/
.angular/

# Env
.env
.env.local

# DB backup taken before the one-submission-per-assignment unique index
*.db.bak-*

# ⚠️ כל גיבוי של מסד הנתונים, בכל סיומת. הריפו ציבורי, ובקובץ הזה יש נתונים אמיתיים
# של תלמידות (שמות, ציונים, גיבובי סיסמאות). קובץ שנדחף פעם אחת נשאר בהיסטוריה לנצח.
*.db.*
